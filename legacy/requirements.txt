# Full application dependencies (Checkpoint 2 onward).
# For the Checkpoint 1 spike, install requirements-spike.txt instead.
PySide6>=6.7
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.26
python-dotenv>=1.0.0
pydantic>=2.6

# LLM provider (Checkpoint 5) - install ONLY the one selected in .env:
# openai>=1.30
# anthropic>=0.34