# Silent Meeting Copilot
Local capture helper (Electron) + hosted web UI (Vercel) + realtime engine (Cloudflare).
Status: dual-stream capture proven (microphone + system loopback). Next: stream to Cloudflare, then the four-panel UI.
